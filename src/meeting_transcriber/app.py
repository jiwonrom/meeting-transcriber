"""Meeting Transcriber — 메인 엔트리포인트."""
from __future__ import annotations

import sys


def main() -> None:
    """앱을 시작한다."""
    # TODO: P0 — PyQt6 앱 초기화
    # 1. QApplication 생성
    # 2. 첫 실행 시 온보딩 (언어 → 모델 다운로드 → 마이크 권한)
    # 3. MainWindow + Tray 생성
    # 4. 이벤트 루프 실행
    print("Meeting Transcriber v1.0.0 — 구현 예정")
    sys.exit(0)


if __name__ == "__main__":
    main()
