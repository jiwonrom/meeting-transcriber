---
phase: 06-system-audio-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-system-audio-capture/02-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SYSAUD-01, SYSAUD-02, SYSAUD-03, SYSAUD-04]
gap_closure: true
must_haves:
  truths:
    - "Phase 2 VERIFICATION.md exists with formal test evidence for all 4 SYSAUD requirements"
    - "REQUIREMENTS.md marks SYSAUD-01 through SYSAUD-04 as [x] (checked)"
    - "ROADMAP.md Phase 2 status is accurate (Complete, 3/3 plans)"
    - "All system audio tests pass (65+ tests across 5 test files)"
  artifacts:
    - path: ".planning/phases/02-system-audio-capture/02-VERIFICATION.md"
      provides: "Formal verification report for Phase 2"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes"
      contains: "[x] **SYSAUD-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate Phase 2 status"
      contains: "Complete"
  key_links:
    - from: "02-VERIFICATION.md"
      to: "test execution output"
      via: "pytest evidence captured in verification report"
      pattern: "VERIFIED|SATISFIED"
---

<objective>
Formally verify all Phase 2 (System Audio Capture) requirements by running tests, reviewing code, creating 02-VERIFICATION.md, and updating REQUIREMENTS.md + ROADMAP.md.

Purpose: Close the verification gap identified by the v2.0 milestone audit. Phase 2 code is complete (3/3 plans executed, 65 tests pass) but was never formally verified -- no VERIFICATION.md exists.
Output: 02-VERIFICATION.md with test evidence, updated REQUIREMENTS.md and ROADMAP.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/06-system-audio-verification/06-RESEARCH.md
@.planning/phases/02-system-audio-capture/02-01-SUMMARY.md
@.planning/phases/02-system-audio-capture/02-02-SUMMARY.md
@.planning/phases/02-system-audio-capture/02-03-SUMMARY.md

<interfaces>
<!-- Use 01-VERIFICATION.md as format reference for the verification report structure. -->
<!-- Read .planning/phases/01-export-multi-provider/01-VERIFICATION.md for the exact format. -->
<!-- Key sections: frontmatter (phase, verified, status, score, re_verification), -->
<!-- Goal Achievement (Observable Truths table, Required Artifacts table, Key Links table), -->
<!-- Test Evidence (captured pytest output), Requirements Traceability (Req ID | Status | Evidence) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all system audio tests and create 02-VERIFICATION.md</name>
  <files>.planning/phases/02-system-audio-capture/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-export-multi-provider/01-VERIFICATION.md (format reference)
    - .planning/phases/02-system-audio-capture/02-01-SUMMARY.md (what was built)
    - .planning/phases/02-system-audio-capture/02-02-SUMMARY.md (what was built)
    - .planning/phases/02-system-audio-capture/02-03-SUMMARY.md (what was built)
    - .planning/phases/06-system-audio-verification/06-RESEARCH.md (test commands and requirement mapping)
    - src/meeting_transcriber/core/system_audio.py (verify detect_blackhole, create_aggregate_device, resolve_device_by_uid exist)
    - src/meeting_transcriber/ui/widgets/toggle_switch.py (verify SystemAudioToggle with toggled/setup_requested signals)
    - src/meeting_transcriber/ui/blackhole_wizard.py (verify BlackHoleSetupWizard 5-step dialog)
    - src/meeting_transcriber/ui/main_window.py (verify start_recording system audio path, _on_system_audio_toggled)
  </read_first>
  <action>
1. Execute the full system audio test suite and capture output:
   `pytest tests/test_system_audio.py tests/test_system_audio_toggle.py tests/test_blackhole_wizard.py tests/test_main_window.py tests/test_settings_dialog.py -x --tb=short -v 2>&amp;1`
   Record total pass count and any failures.

2. Execute requirement-specific test subsets to gather per-requirement evidence:
   - SYSAUD-01: `pytest tests/test_system_audio.py -k "detect_blackhole or is_blackhole" -x -v`
   - SYSAUD-02: `pytest tests/test_blackhole_wizard.py -x -v`
   - SYSAUD-03: `pytest tests/test_system_audio_toggle.py tests/test_main_window.py -k "system_audio" -x -v`
   - SYSAUD-04: `pytest tests/test_system_audio.py -k "aggregate" tests/test_main_window.py -k "system_audio" -x -v`

3. Verify code artifacts exist by reading key source files:
   - Confirm `detect_blackhole()` and `is_blackhole_installed()` functions in `core/system_audio.py`
   - Confirm `create_aggregate_device()` and `destroy_aggregate_device()` in `core/system_audio.py`
   - Confirm `resolve_device_by_uid()` in `core/system_audio.py`
   - Confirm `SystemAudioToggle` class with `toggled` and `setup_requested` signals in `ui/widgets/toggle_switch.py`
   - Confirm `BlackHoleSetupWizard` class in `ui/blackhole_wizard.py`
   - Confirm `DualLevelMeter` class in `ui/widgets/dual_level_meter.py`
   - Confirm `start_recording()` in `ui/main_window.py` contains system audio device resolution logic
   - Confirm `_on_system_audio_toggled` handler in `ui/main_window.py` saves to settings

4. Create `.planning/phases/02-system-audio-capture/02-VERIFICATION.md` following the format of `01-VERIFICATION.md`:

Frontmatter fields: phase=02-system-audio-capture, verified=[current ISO datetime], status=passed, score=[N]/[N] must-haves verified, re_verification=false

Sections to include:
- **Phase Goal:** "Users can capture system audio (the other side of calls) alongside their microphone"
- **Observable Truths** table with 10 rows:
  1. `detect_blackhole()` returns device info dict when BlackHole found in sounddevice list
  2. `is_blackhole_installed()` returns True/False boolean wrapper
  3. `create_aggregate_device()` creates private CoreAudio Aggregate Device with BlackHole as clock master
  4. `destroy_aggregate_device()` removes Aggregate Device by ID
  5. `resolve_device_by_uid()` resolves UID string to integer device index
  6. `SystemAudioToggle` emits `toggled(bool)` on click and `setup_requested` when BlackHole absent
  7. `BlackHoleSetupWizard` provides 5-step guided setup with detection polling
  8. `DualLevelMeter` shows two bars (MIC red, SYS orange) in dual mode
  9. `start_recording()` resolves Aggregate Device when system audio enabled and falls back to mic-only
  10. `_on_system_audio_toggled` persists `audio.system_audio.enabled` to settings
- **Required Artifacts** table listing each source file + test file with existence confirmed
- **Key Links** table showing signal/slot wiring:
  - SystemAudioToggle.toggled -> MainWindow._on_system_audio_toggled
  - SystemAudioToggle.setup_requested -> BlackHoleSetupWizard
  - BlackHoleSetupWizard.setup_completed -> toggle.setChecked(True)
  - start_recording -> resolve_device_by_uid -> AudioCaptureWorker(device=idx)
- **Test Evidence** section with captured pytest output (full suite and per-requirement)
- **Requirements Traceability** table with 4 rows:
  - SYSAUD-01 | SATISFIED | [test names and code evidence for BlackHole detection]
  - SYSAUD-02 | SATISFIED | [test names and code evidence for setup wizard]
  - SYSAUD-03 | SATISFIED | [test names and code evidence for system audio selection]
  - SYSAUD-04 | SATISFIED | [test names and code evidence for dual-channel capture]
  </action>
  <verify>
    <automated>grep -c "SATISFIED" .planning/phases/02-system-audio-capture/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/02-system-audio-capture/02-VERIFICATION.md exists
    - File contains "status: passed" in frontmatter
    - File contains "SYSAUD-01" followed by "SATISFIED" on same row
    - File contains "SYSAUD-02" followed by "SATISFIED" on same row
    - File contains "SYSAUD-03" followed by "SATISFIED" on same row
    - File contains "SYSAUD-04" followed by "SATISFIED" on same row
    - File contains "## Test Evidence" section with pytest output
    - File contains "## Requirements Traceability" section
    - pytest output in file shows 0 failures
  </acceptance_criteria>
  <done>02-VERIFICATION.md exists with PASSED status and all 4 SYSAUD requirements marked SATISFIED with test evidence</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and ROADMAP.md to reflect verified status</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state with [ ] for SYSAUD items)
    - .planning/ROADMAP.md (current Phase 2 and Phase 6 status)
  </read_first>
  <action>
1. In `.planning/REQUIREMENTS.md`, change all four SYSAUD checkboxes from `[ ]` to `[x]`:
   - Line: `- [ ] **SYSAUD-01**:` becomes `- [x] **SYSAUD-01**:`
   - Line: `- [ ] **SYSAUD-02**:` becomes `- [x] **SYSAUD-02**:`
   - Line: `- [ ] **SYSAUD-03**:` becomes `- [x] **SYSAUD-03**:`
   - Line: `- [ ] **SYSAUD-04**:` becomes `- [x] **SYSAUD-04**:`

2. In the Traceability table of REQUIREMENTS.md, update the 4 SYSAUD rows:
   - `| SYSAUD-01 | Phase 6 | Pending |` becomes `| SYSAUD-01 | Phase 6 | Complete |`
   - `| SYSAUD-02 | Phase 6 | Pending |` becomes `| SYSAUD-02 | Phase 6 | Complete |`
   - `| SYSAUD-03 | Phase 6 | Pending |` becomes `| SYSAUD-03 | Phase 6 | Complete |`
   - `| SYSAUD-04 | Phase 6 | Pending |` becomes `| SYSAUD-04 | Phase 6 | Complete |`

3. In `.planning/ROADMAP.md`, update Phase 6 entry:
   - Change plans line to: `**Plans:** 1 plan`
   - Add plan list after the Plans line:
     Plans:
     - [ ] 06-01-PLAN.md -- Formal verification of Phase 2 + documentation updates
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*SYSAUD" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `[x] **SYSAUD-01**`
    - .planning/REQUIREMENTS.md contains `[x] **SYSAUD-02**`
    - .planning/REQUIREMENTS.md contains `[x] **SYSAUD-03**`
    - .planning/REQUIREMENTS.md contains `[x] **SYSAUD-04**`
    - .planning/REQUIREMENTS.md traceability table has `Complete` for all 4 SYSAUD rows
    - .planning/ROADMAP.md Phase 6 section contains `06-01-PLAN.md`
    - grep for "SYSAUD.*Pending" in REQUIREMENTS.md returns 0 matches
  </acceptance_criteria>
  <done>All 4 SYSAUD requirements marked [x] in REQUIREMENTS.md with Complete status in traceability, ROADMAP updated with plan list</done>
</task>

</tasks>

<verification>
1. `grep -c "SATISFIED" .planning/phases/02-system-audio-capture/02-VERIFICATION.md` returns 4 (one per SYSAUD requirement)
2. `grep -c "\[x\] \*\*SYSAUD" .planning/REQUIREMENTS.md` returns 4
3. `grep "SYSAUD.*Pending" .planning/REQUIREMENTS.md` returns no matches
4. `pytest tests/test_system_audio.py tests/test_system_audio_toggle.py tests/test_blackhole_wizard.py -x --tb=short -q` passes
</verification>

<success_criteria>
- 02-VERIFICATION.md exists in Phase 2 directory with status: passed and all 4 SYSAUD requirements SATISFIED
- REQUIREMENTS.md has all 4 SYSAUD items checked [x] with Complete status
- ROADMAP.md accurately reflects Phase 6 plan structure
- All system audio tests pass (65+ tests, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-audio-verification/06-01-SUMMARY.md`
</output>
