---
phase: 05-cross-meeting-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/meeting_transcriber/ui/sidebar.py
  - tests/test_sidebar.py
autonomous: true
requirements:
  - CMA-01

must_haves:
  truths:
    - "User can enter selection mode via a toolbar button"
    - "Checkboxes appear next to each transcript in selection mode"
    - "Cross-folder selection works -- transcripts from any folder can be checked"
    - "Folder-level checkbox selects/deselects all children transcripts"
    - "Select All and Cancel buttons appear in selection mode"
    - "Sticky 'Analyze N selected' bar appears when >= 2 transcripts checked"
    - "Selection mode can be exited, removing all checkboxes"
  artifacts:
    - path: "src/meeting_transcriber/ui/sidebar.py"
      provides: "Selection mode with checkboxes, action bar, analyses section"
      min_lines: 200
    - path: "tests/test_sidebar.py"
      provides: "Tests for selection mode behavior"
  key_links:
    - from: "src/meeting_transcriber/ui/sidebar.py"
      to: "MainWindow (via signal)"
      via: "analysis_requested pyqtSignal(list)"
      pattern: "analysis_requested"
    - from: "src/meeting_transcriber/ui/sidebar.py"
      to: "src/meeting_transcriber/utils/constants.py"
      via: "MIN_SELECTION_COUNT import"
      pattern: "MIN_SELECTION_COUNT"
---

<objective>
Add multi-transcript selection mode to SidebarWidget with checkboxes, folder-level selection, action bar, and a dedicated "Analyses" section for browsing saved analyses.

Purpose: Enable users to select multiple transcripts for cross-meeting analysis (CMA-01).
Output: Updated sidebar.py with selection mode, updated test_sidebar.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-meeting-analysis/05-CONTEXT.md
@.planning/phases/05-cross-meeting-analysis/05-RESEARCH.md

<interfaces>
<!-- SidebarWidget current structure -->

From src/meeting_transcriber/ui/sidebar.py:
```python
class SidebarWidget(QWidget):
    folder_selected = pyqtSignal(str)
    transcript_selected = pyqtSignal(str)
    folder_created = pyqtSignal(str)
    folder_renamed = pyqtSignal(str, str)
    folder_deleted = pyqtSignal(str)

    # Internal model uses:
    # item.setData(str(folder.path), Qt.ItemDataRole.UserRole)       -- path
    # item.setData("folder", Qt.ItemDataRole.UserRole + 1)           -- type
    # child.setData(str(path), Qt.ItemDataRole.UserRole)             -- transcript path
    # child.setData("transcript", Qt.ItemDataRole.UserRole + 1)      -- type
```

From src/meeting_transcriber/utils/constants.py (after Plan 01):
```python
MIN_SELECTION_COUNT = 2
ANALYSES_DIR = "analyses"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar selection mode with checkboxes and action bar</name>
  <read_first>
    - src/meeting_transcriber/ui/sidebar.py
    - tests/test_sidebar.py
    - src/meeting_transcriber/utils/constants.py
  </read_first>
  <files>
    src/meeting_transcriber/ui/sidebar.py,
    tests/test_sidebar.py
  </files>
  <action>
    **sidebar.py changes:**

    1. **New signals** -- Add to class-level signals:
       - `analysis_requested = pyqtSignal(list)` -- emits list[str] of transcript paths when user clicks "Analyze N selected"
       - `analysis_selected = pyqtSignal(str)` -- emits analysis file path when user clicks a saved analysis in the Analyses section

    2. **New imports** at top:
       - `from meeting_transcriber.utils.constants import MIN_SELECTION_COUNT, ANALYSES_DIR`
       - Add `QLabel` to PyQt6.QtWidgets imports if not present
       - Add `QPushButton` if not already imported

    3. **Instance variables** in `__init__`:
       - `self._selection_mode: bool = False`
       - `self._action_bar: QWidget | None = None` -- lazy-created sticky bar
       - `self._action_label: QLabel | None = None` -- "Analyze N selected" label
       - `self._select_btn: QPushButton | None = None` -- toolbar button

    4. **Modify `_setup_ui`** to add "Select" button in toolbar:
       - Per D-01: Add a `QPushButton("Select")` next to the existing "+ New Folder" button in the toolbar layout. Store as `self._select_btn`.
       - Connect `self._select_btn.clicked` to `self._on_select_btn_clicked`.
       - Create the action bar (initially hidden): a QWidget at the bottom of the sidebar layout with:
         - QHBoxLayout containing:
           - `self._action_label = QLabel("")` 
           - `self._analyze_btn = QPushButton("Analyze")` connected to `self._on_analyze_clicked`
         - Set `self._action_bar.setVisible(False)` initially
       - Per D-04: Add "Select All" and "Cancel" buttons -- these appear when in selection mode. Create `self._select_all_btn = QPushButton("Select All")` and `self._cancel_btn = QPushButton("Cancel")`, initially hidden. Connect to `_on_select_all_clicked` and `_on_cancel_clicked`. Place these in the toolbar area, shown only during selection mode.

    5. **Selection mode methods:**
       - `_on_select_btn_clicked(self) -> None` -- calls `_enter_selection_mode()`
       - `_enter_selection_mode(self) -> None`:
         - Set `self._selection_mode = True`
         - Hide "Select" and "+ New Folder" buttons
         - Show "Select All" and "Cancel" buttons
         - Iterate all items in `self._model`: for each folder item (`rowCount` of model), set `folder_item.setCheckable(True)` and `folder_item.setCheckState(Qt.CheckState.Unchecked)`. For each child of folder where child.data(UserRole+1) == "transcript", set `child.setCheckable(True)` and `child.setCheckState(Qt.CheckState.Unchecked)`.
         - Connect `self._model.itemChanged` to `self._on_item_check_changed`
         - Show the action bar (hidden, will show when count >= 2)
       - `_exit_selection_mode(self) -> None`:
         - Set `self._selection_mode = False`
         - Show "Select" and "+ New Folder" buttons
         - Hide "Select All" and "Cancel" buttons
         - Iterate all items, set `setCheckable(False)` for all folder and transcript items
         - Disconnect `self._model.itemChanged` from `self._on_item_check_changed`
         - Hide action bar
       - `_on_cancel_clicked(self) -> None` -- calls `_exit_selection_mode()`
       - `_on_select_all_clicked(self) -> None`:
         - Per D-04: iterate all transcript items across all folders, set `child.setCheckState(Qt.CheckState.Checked)`
         - Update folder states and action bar count

    6. **Checkbox propagation** (per Research pitfall 1):
       - `_on_item_check_changed(self, item: QStandardItem) -> None`:
         - If item type is "folder" (UserRole+1 == "folder"):
           - Propagate to all children: set each child's checkState to match folder's checkState (Checked or Unchecked). Block signals during propagation to avoid recursion.
         - If item type is "transcript":
           - Check parent folder: if ALL children checked -> parent Checked; if SOME checked -> parent PartiallyChecked; if NONE checked -> parent Unchecked. Block signals during parent update.
         - Call `self._update_action_bar()`
       - `_update_action_bar(self) -> None`:
         - Count checked transcripts via `_get_checked_transcript_paths()`
         - If count >= MIN_SELECTION_COUNT: show action bar, update label text to `f"Analyze {count} selected"`
         - If count < MIN_SELECTION_COUNT: hide action bar

    7. **Transcript path collection:**
       - `_get_checked_transcript_paths(self) -> list[str]`:
         - Per D-02 (cross-folder): iterate ALL folders in model, collect paths of all checked transcript items
         - Return list of path strings from `item.data(Qt.ItemDataRole.UserRole)`

    8. **Analyze trigger:**
       - `_on_analyze_clicked(self) -> None`:
         - Get paths from `_get_checked_transcript_paths()`
         - If len >= MIN_SELECTION_COUNT: emit `self.analysis_requested.emit(paths)`
         - Exit selection mode after emitting

    9. **Analyses section** (per D-22):
       - In `refresh()` method, after loading folders and transcripts, add an "Analyses" section at the bottom of the model:
         - Create a `QStandardItem("Analyses")` with bold font, non-checkable, non-editable
         - Set its data: `item.setData("analyses_header", Qt.ItemDataRole.UserRole + 1)`
         - Scan `workspace_root / ANALYSES_DIR` for `analysis_*.json` files
         - For each file, add a child item with display name derived from filename (strip "analysis_" prefix and ".json" suffix, format timestamp nicely e.g. "2026-03-31 14:00:05")
         - Child item data: `child.setData(str(path), Qt.ItemDataRole.UserRole)`, `child.setData("analysis", Qt.ItemDataRole.UserRole + 1)`
       - In `_on_item_clicked`: if item_type == "analysis", emit `self.analysis_selected.emit(item_path)`
       - Analyses items should NOT be checkable even in selection mode

    10. **Modify `refresh()`** to preserve selection mode state:
        - If `self._selection_mode` is True when refresh is called, after rebuilding the model, re-apply checkable state to all new items. Store checked paths before refresh, restore after.

    **tests/test_sidebar.py changes:**

    Add new test functions (keep existing tests untouched):

    - `test_selection_mode_enter_exit(sidebar_widget)` -- call `_enter_selection_mode()`, verify `_selection_mode` is True. Call `_exit_selection_mode()`, verify False.
    - `test_selection_mode_checkboxes_appear(sidebar_widget)` -- enter selection mode, iterate model items, verify all transcript items are checkable.
    - `test_folder_checkbox_propagates_to_children(sidebar_widget)` -- enter selection mode, check a folder item, verify all its transcript children are Checked.
    - `test_action_bar_visible_when_two_selected(sidebar_widget)` -- enter selection mode, check 2 transcript items, verify action bar is visible.
    - `test_action_bar_hidden_when_less_than_two(sidebar_widget)` -- enter selection mode, check only 1 transcript, verify action bar is hidden.
    - `test_analysis_requested_signal(sidebar_widget, qtbot)` -- enter selection mode, check 2+ transcripts, call `_on_analyze_clicked`, use `qtbot.waitSignal(sidebar_widget.analysis_requested)` to verify signal emitted with correct paths.
    - `test_analyses_section_in_tree(sidebar_widget, tmp_path)` -- create an analysis file in analyses/ dir, call refresh, verify "Analyses" item exists in model with the analysis as child.

    Note: the existing `sidebar_widget` fixture likely uses a mock WorkspaceManager. Ensure new tests work with it or create a fixture variant that includes analyses dir.
  </action>
  <verify>
    <automated>cd /Users/jiwon/Documents/Dev/MeetingTranscriber && python -m pytest tests/test_sidebar.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.py contains `analysis_requested = pyqtSignal(list)`
    - sidebar.py contains `analysis_selected = pyqtSignal(str)`
    - sidebar.py contains `def _enter_selection_mode(self) -> None:`
    - sidebar.py contains `def _exit_selection_mode(self) -> None:`
    - sidebar.py contains `def _on_item_check_changed(self, item: QStandardItem) -> None:`
    - sidebar.py contains `def _get_checked_transcript_paths(self) -> list[str]:`
    - sidebar.py contains `def _on_analyze_clicked(self) -> None:`
    - sidebar.py contains `MIN_SELECTION_COUNT`
    - sidebar.py contains `"Analyses"`
    - sidebar.py contains `"analysis"` (item type)
    - test_sidebar.py contains `test_selection_mode_enter_exit`
    - test_sidebar.py contains `test_action_bar_visible_when_two_selected`
    - test_sidebar.py contains `test_analysis_requested_signal`
    - pytest tests/test_sidebar.py exits 0
  </acceptance_criteria>
  <done>SidebarWidget has selection mode with checkboxes, folder-level propagation, sticky action bar showing "Analyze N selected" for >= 2 checked transcripts, and an Analyses section for browsing saved analyses. All sidebar tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/jiwon/Documents/Dev/MeetingTranscriber && python -m pytest tests/test_sidebar.py -x --tb=short -v
```

Verify no ruff issues:
```bash
cd /Users/jiwon/Documents/Dev/MeetingTranscriber && python -m ruff check src/meeting_transcriber/ui/sidebar.py
```
</verification>

<success_criteria>
- Sidebar enters/exits selection mode with toolbar button
- Checkboxes appear on all transcript items in selection mode
- Folder checkbox propagates to children and vice versa
- "Analyze N selected" bar appears when >= 2 transcripts checked
- analysis_requested signal emits list of transcript paths
- Analyses section shows saved analysis files below folder tree
- All test_sidebar.py tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-meeting-analysis/05-02-SUMMARY.md`
</output>
