---
phase: 02-system-audio-capture
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/meeting_transcriber/ui/widgets/__init__.py
  - src/meeting_transcriber/ui/widgets/toggle_switch.py
  - src/meeting_transcriber/ui/widgets/dual_level_meter.py
  - src/meeting_transcriber/ui/blackhole_wizard.py
  - src/meeting_transcriber/ui/__init__.py
  - design/tokens_dark.json
  - design/tokens_light.json
  - tests/test_system_audio_toggle.py
  - tests/test_blackhole_wizard.py
autonomous: true
requirements: [SYSAUD-01, SYSAUD-02, SYSAUD-03]

must_haves:
  truths:
    - "SystemAudioToggle renders a 44x24px toggle switch with QPainter"
    - "SystemAudioToggle emits toggled(bool) signal on click when enabled"
    - "SystemAudioToggle shows ForbiddenCursor and opens wizard when disabled (no BlackHole)"
    - "DualLevelMeter shows single mic bar in mic-only mode, both bars in dual mode"
    - "BlackHoleSetupWizard navigates through 5 steps with Back/Next buttons"
    - "BlackHoleSetupWizard step 2 polls for BlackHole detection every 2 seconds"
    - "BlackHoleSetupWizard step 3 guides user to route system audio output through BlackHole"
    - "BlackHoleSetupWizard step 4 triggers Aggregate Device creation"
  artifacts:
    - path: "src/meeting_transcriber/ui/widgets/toggle_switch.py"
      provides: "SystemAudioToggle custom QPainter widget"
      exports: ["SystemAudioToggle"]
    - path: "src/meeting_transcriber/ui/widgets/dual_level_meter.py"
      provides: "DualLevelMeter stacked level bars"
      exports: ["DualLevelMeter"]
    - path: "src/meeting_transcriber/ui/blackhole_wizard.py"
      provides: "5-step BlackHole setup wizard"
      exports: ["BlackHoleSetupWizard"]
    - path: "tests/test_system_audio_toggle.py"
      provides: "Toggle widget tests"
    - path: "tests/test_blackhole_wizard.py"
      provides: "Wizard UI tests"
  key_links:
    - from: "src/meeting_transcriber/ui/blackhole_wizard.py"
      to: "src/meeting_transcriber/core/system_audio.py"
      via: "import detect_blackhole, create_aggregate_device, get_device_uid"
      pattern: "from meeting_transcriber\\.core\\.system_audio import"
    - from: "src/meeting_transcriber/ui/widgets/toggle_switch.py"
      to: "PyQt6.QtCore.pyqtSignal"
      via: "toggled = pyqtSignal(bool)"
      pattern: "toggled = pyqtSignal"
---

<objective>
Create the three new UI widgets for system audio capture: SystemAudioToggle (custom QPainter toggle switch), DualLevelMeter (stacked mic + system level bars), and BlackHoleSetupWizard (5-step QDialog including audio output routing guidance). Also extend design tokens.

Purpose: These widgets are the visual building blocks that Plan 03 will wire into MainWindow and the settings dialog.
Output: Three new widget files, two test files, updated design tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-system-audio-capture/02-CONTEXT.md
@.planning/phases/02-system-audio-capture/02-UI-SPEC.md
@.planning/phases/02-system-audio-capture/02-RESEARCH.md

@src/meeting_transcriber/ui/onboarding.py
@src/meeting_transcriber/ui/main_window.py
@src/meeting_transcriber/ui/theme.py
@design/tokens_dark.json
@design/tokens_light.json

<interfaces>
<!-- From Plan 01 (must be complete before this plan executes) -->

From src/meeting_transcriber/core/system_audio.py:
```python
def detect_blackhole() -> int | None: ...
def is_blackhole_installed() -> bool: ...
def get_device_uid(device_index: int) -> str: ...
def create_aggregate_device(mic_uid: str, blackhole_uid: str) -> int: ...
def destroy_aggregate_device(device_id: int) -> None: ...
def resolve_device_by_uid(uid: str) -> int | None: ...
```

From src/meeting_transcriber/utils/constants.py:
```python
BLACKHOLE_DEVICE_NAMES = ("blackhole 2ch", "blackhole 16ch", "blackhole 64ch")
AGGREGATE_DEVICE_NAME = "Scribe Audio (Mic + System)"
AGGREGATE_DEVICE_UID = "com.scribe.aggregate-device"
```

From src/meeting_transcriber/utils/exceptions.py:
```python
class SystemAudioError(MeetingTranscriberError): ...
```

<!-- Existing UI patterns -->
From src/meeting_transcriber/ui/main_window.py (RecordButton pattern):
```python
class RecordButton(QPushButton):
    def __init__(self, parent=None):
        self.setFixedSize(56, 56)
        self.setCursor(Qt.CursorShape.PointingHandCursor)
    def paintEvent(self, event): ...  # QPainter custom drawing
```

From src/meeting_transcriber/ui/onboarding.py (Wizard pattern):
```python
class OnboardingWizard(QDialog):
    def __init__(self, parent=None):
        self.setFixedSize(500, 400)
        self.setWindowFlags(self.windowFlags() & ~Qt.WindowType.WindowContextHelpButtonHint)
        self._stack = QStackedWidget()
        # Back/Next buttons in bottom layout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SystemAudioToggle and DualLevelMeter widgets + design token updates</name>
  <files>
    src/meeting_transcriber/ui/widgets/__init__.py,
    src/meeting_transcriber/ui/widgets/toggle_switch.py,
    src/meeting_transcriber/ui/widgets/dual_level_meter.py,
    design/tokens_dark.json,
    design/tokens_light.json,
    tests/test_system_audio_toggle.py
  </files>
  <read_first>
    src/meeting_transcriber/ui/main_window.py,
    src/meeting_transcriber/ui/theme.py,
    src/meeting_transcriber/ui/onboarding.py,
    design/tokens_dark.json,
    design/tokens_light.json,
    .planning/phases/02-system-audio-capture/02-UI-SPEC.md,
    tests/conftest.py
  </read_first>
  <action>
1. **Create `src/meeting_transcriber/ui/widgets/` directory** with `__init__.py` (empty, docstring only: `"""UI 커스텀 위젯."""`).

2. **Create `src/meeting_transcriber/ui/widgets/toggle_switch.py`** — `SystemAudioToggle(QWidget)`:
   - Module docstring: `"""시스템 오디오 토글 스위치 — QPainter 커스텀 위젯."""`
   - `from __future__ import annotations`
   - Imports: `QWidget`, `QLabel`, `Qt`, `QTimer`, `pyqtSignal`, `QPropertyAnimation`, `QEasingCurve`, `QPainter`, `QColor`, `QRectF`, `QVBoxLayout` from PyQt6
   - Signal: `toggled = pyqtSignal(bool)`
   - Signal: `setup_requested = pyqtSignal()` — emitted when disabled toggle is clicked (opens wizard)
   - `__init__(self, parent=None)`:
     - `self.setFixedSize(44, 24)` — toggle track only; label is separate
     - `self._checked = False`
     - `self._enabled = True` (custom enabled, separate from QWidget.setEnabled)
     - `self._blackhole_available = False`
     - `self._recording = False` — locks toggle during recording
     - `self._thumb_position = 0.0` — animated: 0.0=left/off, 1.0=right/on
     - Set up `QPropertyAnimation` on a custom Q_PROPERTY `thumb_position` (use `pyqtProperty(float)` decorator). Duration 150ms, `QEasingCurve.Type.InOutQuad`.
     - `self.setCursor(Qt.CursorShape.PointingHandCursor)`
     - Tooltip: `"Capture system audio alongside microphone"`
   - Colors (hardcoded defaults, will be set by theme):
     - `_track_off_color = QColor("#3A3A3C")` (background.control)
     - `_track_on_color = QColor("#FF453A")` (status.recording)
     - `_thumb_color = QColor("#F5F5F7")` (text.primary)
     - `_disabled_track_color = QColor(58, 58, 60, 128)` (50% opacity)
     - `_disabled_thumb_color = QColor("#48484A")` (text.muted)
   - `set_theme_colors(self, track_off: str, track_on: str, thumb: str, disabled_track_rgba: tuple, disabled_thumb: str)` — update color attributes
   - `set_blackhole_available(self, available: bool)` — updates `_blackhole_available`, changes cursor to ForbiddenCursor if False (and not recording), updates tooltip
   - `set_recording(self, recording: bool)` — sets `_recording`, updates cursor and tooltip accordingly
   - `setChecked(self, checked: bool)` — programmatically set state, animate thumb, emit toggled
   - `isChecked(self) -> bool` — return `_checked`
   - `mousePressEvent(self, event)`:
     - If `_recording`: ignore (tooltip says "Cannot change audio source while recording")
     - If not `_blackhole_available`: emit `setup_requested` signal (per D-04), do NOT toggle
     - Else: toggle `_checked`, animate thumb position (0.0 -> 1.0 or 1.0 -> 0.0), emit `toggled(self._checked)`
   - `paintEvent(self, event)`:
     - Draw track: rounded rect 44x24, border-radius 12px
     - If disabled/not available: use disabled colors at 50% opacity
     - If checked/on: use `_track_on_color`, thumb = white (#FFFFFF)
     - If unchecked/off: use `_track_off_color`, thumb = `_thumb_color`
     - Draw thumb: 20x20 circle at x = 2 + (thumb_position * 20), y = 2. The 2px is the inset.
     - Use `QPainter.RenderHint.Antialiasing`

3. **Create `src/meeting_transcriber/ui/widgets/dual_level_meter.py`** — `DualLevelMeter(QWidget)`:
   - Module docstring: `"""듀얼 레벨 미터 — 마이크 + 시스템 오디오."""`
   - Contains two `QProgressBar` widgets stacked vertically
   - `__init__(self, parent=None)`:
     - `QVBoxLayout` with `setSpacing(4)` (xs token) and `setContentsMargins(0,0,0,0)`
     - Mic bar: `QProgressBar`, objectName `"mic_level_bar"`, range 0-100, fixedHeight 4, textVisible False
     - System bar: `QProgressBar`, objectName `"system_level_bar"`, range 0-100, fixedHeight 4, textVisible False
     - Optional labels: `QLabel("MIC")` and `QLabel("SYS")`, objectName `"caption"`, 11px, initially hidden
     - System bar initially hidden: `self._system_bar.setVisible(False)`, system label hidden too
   - `set_dual_mode(self, dual: bool)` — shows/hides system bar + labels. When dual=True, widget height is 12px (4+4+4 spacing). When False, height is 4px.
   - `set_mic_level(self, level: float)` — `self._mic_bar.setValue(int(level * 100))`
   - `set_system_level(self, level: float)` — `self._system_bar.setValue(int(level * 100))`
   - `reset(self)` — set both bars to 0

4. **Update `design/tokens_dark.json`**: Extend the `"controlBar"` section to add:
```json
"controlBar": {
    "height": 72,
    "heightDual": 80,
    "buttonSize": 56,
    "levelBarHeight": 4,
    "toggleWidth": 44,
    "toggleHeight": 24,
    "toggleThumbSize": 20,
    "toggleBorderRadius": 12
}
```

5. **Update `design/tokens_light.json`**: Same `controlBar` extension as dark tokens.

6. **Create `tests/test_system_audio_toggle.py`**:
   - `test_toggle_emits_signal(qtbot)`: Create toggle, set blackhole_available=True, click, assert `toggled` signal emitted with True
   - `test_toggle_disabled_emits_setup_requested(qtbot)`: Create toggle, blackhole_available=False, click, assert `setup_requested` signal emitted, `toggled` NOT emitted
   - `test_toggle_locked_during_recording(qtbot)`: Create toggle, set_recording(True), click, assert no signals emitted
   - `test_toggle_initial_state(qtbot)`: Assert isChecked() is False, _thumb_position is 0.0
   - `test_dual_level_meter_single_mode(qtbot)`: Create meter, assert system bar hidden
   - `test_dual_level_meter_dual_mode(qtbot)`: Create meter, set_dual_mode(True), assert system bar visible
  </action>
  <verify>
    <automated>cd /Users/jiwon/Documents/Dev/MeetingTranscriber && python -m pytest tests/test_system_audio_toggle.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - File `src/meeting_transcriber/ui/widgets/__init__.py` exists
    - File `src/meeting_transcriber/ui/widgets/toggle_switch.py` exists and contains `class SystemAudioToggle(QWidget):`
    - toggle_switch.py contains `toggled = pyqtSignal(bool)`
    - toggle_switch.py contains `setup_requested = pyqtSignal()`
    - toggle_switch.py contains `self.setFixedSize(44, 24)`
    - toggle_switch.py contains `def paintEvent(`
    - toggle_switch.py contains `def set_blackhole_available(`
    - toggle_switch.py contains `def set_recording(`
    - File `src/meeting_transcriber/ui/widgets/dual_level_meter.py` exists and contains `class DualLevelMeter(QWidget):`
    - dual_level_meter.py contains `objectName` set to `"mic_level_bar"` and `"system_level_bar"`
    - dual_level_meter.py contains `def set_dual_mode(`
    - dual_level_meter.py contains `def set_mic_level(`
    - dual_level_meter.py contains `def set_system_level(`
    - `design/tokens_dark.json` controlBar section contains `"toggleWidth": 44`
    - `design/tokens_light.json` controlBar section contains `"toggleWidth": 44`
    - `pytest tests/test_system_audio_toggle.py -x` exits with code 0
  </acceptance_criteria>
  <done>SystemAudioToggle and DualLevelMeter widgets exist with QPainter rendering, correct signals, and tested behavior. Design tokens updated.</done>
</task>

<task type="auto">
  <name>Task 2: Create BlackHoleSetupWizard 5-step QDialog with audio output routing guidance</name>
  <files>
    src/meeting_transcriber/ui/blackhole_wizard.py,
    src/meeting_transcriber/ui/__init__.py,
    tests/test_blackhole_wizard.py
  </files>
  <read_first>
    src/meeting_transcriber/ui/onboarding.py,
    src/meeting_transcriber/core/system_audio.py,
    .planning/phases/02-system-audio-capture/02-UI-SPEC.md,
    .planning/phases/02-system-audio-capture/02-CONTEXT.md,
    .planning/phases/02-system-audio-capture/02-RESEARCH.md,
    tests/conftest.py
  </read_first>
  <action>
Create `src/meeting_transcriber/ui/blackhole_wizard.py` following the exact OnboardingWizard pattern from `onboarding.py`:

Module docstring: `"""BlackHole 설치 및 Aggregate Device 생성 위저드."""`

**Class: `AggregateDeviceThread(QThread)`**
- Signal: `creation_finished = pyqtSignal(bool, str, int)` — (success, message, device_id)
- `__init__(self, mic_uid: str, blackhole_uid: str, parent=None)`
- `run(self)`: Import and call `create_aggregate_device(mic_uid, blackhole_uid)`. On success emit `(True, "Aggregate Device created!", device_id)`. On SystemAudioError emit `(False, str(e), 0)`. On any other Exception emit `(False, str(e), 0)`.

**Class: `BlackHoleSetupWizard(QDialog)`**
- Signal: `setup_completed = pyqtSignal()` — emitted when wizard finishes successfully
- `__init__(self, parent=None)`:
  - `self.setWindowTitle("Scribe — System Audio Setup")`
  - `self.setFixedSize(500, 520)` — taller to accommodate the extra step
  - `self.setWindowFlags(self.windowFlags() & ~Qt.WindowType.WindowContextHelpButtonHint)`
  - `self._detection_timer: QTimer | None = None`
  - `self._creation_thread: AggregateDeviceThread | None = None`
  - `self._aggregate_device_id: int = 0`

- `_setup_ui(self)`:
  - QVBoxLayout, QStackedWidget with 5 pages
  - Bottom nav: Back (left, hidden on step 0), step indicator "Step N of 5" (center), Next (right)
  - Back button initially hidden, visible from step 1 onward

**Step 1 — Introduction (`_create_intro_page`):**
  - Heading (17px, semibold): "Capture System Audio"
  - Body (14px): "To transcribe the other side of calls, Scribe needs BlackHole — a free virtual audio driver."
  - Illustration: 200x120px QWidget with `background.elevated` fill and QPainter line art (two sine waves merging into one — simple lines, not elaborate)
  - Next button label: "Get Started"

**Step 2 — Install BlackHole (`_create_install_page`):**
  - Heading: "Install BlackHole"
  - Option A card: QFrame objectName `"install_card"`, border with `border.default`, border-radius 6px, padding 16px. Contains label "Install via Homebrew", monospace QLabel `brew install blackhole-2ch`, QPushButton "Copy Command" (copies to clipboard via `QApplication.clipboard().setText("brew install blackhole-2ch")`)
  - Option B card: QFrame objectName `"install_card"`. Contains label "Download from GitHub", QPushButton "Open Download Page" (opens `https://github.com/ExistentialAudio/BlackHole/releases` via `QDesktopServices.openUrl`)
  - Status label below: "Waiting for BlackHole installation..." (text.tertiary, 11px)
  - QTimer polling: `self._detection_timer = QTimer()`, interval 2000ms, connected to `_poll_blackhole_detection()`
  - `_poll_blackhole_detection(self)`: calls `detect_blackhole()`. If not None, stop timer, update status to checkmark + "BlackHole detected!" (green), enable Next button.
  - Next button: disabled until detection. Label: "Continue"
  - Timer starts when page becomes visible (use `_on_page_changed` or start in `_go_next` when moving to page 1)

**Step 3 — Route System Audio Output (`_create_audio_output_page`):**
  (Addresses RESEARCH.md Pitfall 5 -- users need system audio routed through BlackHole for capture to work. Without this step, SYSAUD-03 and SYSAUD-04 will not function because system audio output is not going through BlackHole.)
  - Heading: "Route System Audio"
  - Body (14px): "For Scribe to hear system audio, your Mac's sound output must include BlackHole. Open Sound Settings and create a Multi-Output Device that sends audio to both your speakers and BlackHole."
  - Instruction list (numbered, 13px):
    1. "Open Sound Settings below"
    2. "Go to Output and select 'Multi-Output Device' (or create one in Audio MIDI Setup)"
    3. "Ensure both your speakers/headphones AND BlackHole 2ch are checked"
  - QPushButton "Open Sound Settings" — opens `QDesktopServices.openUrl(QUrl("x-apple.systempreferences:com.apple.preference.sound"))` (macOS Sound preference pane URL). If that URL scheme doesn't work, fall back to `QDesktopServices.openUrl(QUrl("file:///System/Applications/Utilities/Audio%20MIDI%20Setup.app"))`.
  - QPushButton "Open Audio MIDI Setup" — opens `QDesktopServices.openUrl(QUrl("file:///System/Applications/Utilities/Audio%20MIDI%20Setup.app"))` as alternative for users who need to create the Multi-Output Device.
  - Hint label (text.tertiary, 11px): "Tip: In Audio MIDI Setup, click '+' at the bottom left, select 'Create Multi-Output Device', then check your speakers and BlackHole 2ch."
  - Next button: always enabled (we cannot programmatically verify Multi-Output Device configuration). Label: "I've set it up"

**Step 4 — Create Aggregate Device (`_create_aggregate_page`):**
  - Heading: "Set Up Audio Mixing"
  - Body: "Scribe will create an Aggregate Device that combines your microphone and BlackHole into a single input."
  - Device name preview: QFrame with monospace label "Scribe Audio (Mic + System)"
  - Action button (centered): "Create Aggregate Device"
  - On click: get default mic device index from `sd.default.device[0]`, resolve UIDs via `get_device_uid()`, get BlackHole UID via `get_device_uid(detect_blackhole())`. Launch `AggregateDeviceThread(mic_uid, blackhole_uid)`.
  - Loading state: button disabled, text "Creating...", indeterminate QProgressBar shown
  - On `creation_finished`:
    - Success: hide progress, show checkmark + "Aggregate Device created!", enable Next, store device_id and UIDs in settings via `save_settings()`
    - Error: hide progress, show error message in red + "Retry" button, log error
  - Next button: disabled until device created. Label: "Continue"

**Step 5 — Complete (`_create_complete_page`):**
  - Heading: "All Set!"
  - Body: "System audio capture is ready. Toggle 'System Audio' next to the record button to capture both sides of your calls."
  - QPainter success icon: 48x48 green circle with white checkmark
  - Finish button label: "Done"
  - On finish: update settings `audio.system_audio.enabled = True`, emit `setup_completed`, `self.accept()`

**Navigation (_go_next, _go_back):**
  - Follow exact OnboardingWizard pattern
  - _go_next: increment stack index, update button visibility/text, start timer on page 1
  - _go_back: decrement stack index, hide back on page 0
  - Step indicator updates: `self._step_label.setText(f"Step {index + 1} of 5")`

**Settings update on completion:**
```python
settings = load_settings()
settings["audio"]["system_audio"]["enabled"] = True
settings["audio"]["system_audio"]["blackhole_uid"] = blackhole_uid
settings["audio"]["system_audio"]["aggregate_device_uid"] = AGGREGATE_DEVICE_UID
settings["audio"]["system_audio"]["mic_device_uid"] = mic_uid
save_settings(settings)
```

**Create `tests/test_blackhole_wizard.py`**:
- `test_wizard_opens(qtbot)`: Create wizard, assert visible, 5 pages in stack
- `test_wizard_step_navigation(qtbot)`: Click Next from step 0, assert stack index is 1, Back visible
- `test_wizard_back_navigation(qtbot)`: Go to step 1, click Back, assert stack index 0, Back hidden
- `test_wizard_step_indicator(qtbot)`: Assert step label text updates on navigation ("Step 1 of 5" through "Step 5 of 5")
- `test_wizard_copy_command(qtbot, mocker)`: Mock clipboard, click copy button on step 1, assert clipboard contains "brew install blackhole-2ch"
- `test_wizard_detection_polling(qtbot, mocker)`: Mock `detect_blackhole` returning None then index, advance timer, assert status label changes
- `test_wizard_audio_output_page_has_buttons(qtbot)`: Assert step 2 (audio output routing page) contains "Open Sound Settings" and "Open Audio MIDI Setup" buttons
- `test_wizard_aggregate_creation_success(qtbot, mocker)`: Mock AggregateDeviceThread success, assert next enabled on step 3

All tests mock `detect_blackhole`, `create_aggregate_device`, `get_device_uid` to avoid real CoreAudio calls.
  </action>
  <verify>
    <automated>cd /Users/jiwon/Documents/Dev/MeetingTranscriber && python -m pytest tests/test_blackhole_wizard.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - File `src/meeting_transcriber/ui/blackhole_wizard.py` exists
    - blackhole_wizard.py contains `class BlackHoleSetupWizard(QDialog):`
    - blackhole_wizard.py contains `class AggregateDeviceThread(QThread):`
    - blackhole_wizard.py contains `setup_completed = pyqtSignal()`
    - blackhole_wizard.py contains `self.setFixedSize(500, 520)`
    - blackhole_wizard.py contains `self.setWindowTitle("Scribe — System Audio Setup")`
    - blackhole_wizard.py contains `"brew install blackhole-2ch"` (copy command)
    - blackhole_wizard.py contains `QTimer` for detection polling
    - blackhole_wizard.py contains `from meeting_transcriber.core.system_audio import`
    - blackhole_wizard.py contains `save_settings` call with system_audio settings
    - blackhole_wizard.py contains `"Route System Audio"` heading (audio output routing step)
    - blackhole_wizard.py contains `"Open Sound Settings"` button text
    - blackhole_wizard.py contains `"Open Audio MIDI Setup"` button text
    - blackhole_wizard.py contains `QDesktopServices.openUrl` for opening Sound Settings
    - blackhole_wizard.py contains `"Step {N} of 5"` step indicator pattern (5 steps, not 4)
    - tests/test_blackhole_wizard.py contains at least 6 test functions
    - `pytest tests/test_blackhole_wizard.py -x` exits with code 0
  </acceptance_criteria>
  <done>BlackHoleSetupWizard exists with 5 navigable steps (including audio output routing guidance per RESEARCH.md Pitfall 5), BlackHole detection polling, Aggregate Device creation via background thread, and settings persistence. All tested.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_system_audio_toggle.py tests/test_blackhole_wizard.py -x --tb=short -v` — all pass
- `python -c "from meeting_transcriber.ui.widgets.toggle_switch import SystemAudioToggle"` — imports
- `python -c "from meeting_transcriber.ui.widgets.dual_level_meter import DualLevelMeter"` — imports
- `python -c "from meeting_transcriber.ui.blackhole_wizard import BlackHoleSetupWizard"` — imports
</verification>

<success_criteria>
- SystemAudioToggle renders 44x24 toggle with QPainter, emits toggled/setup_requested signals correctly
- DualLevelMeter switches between single (4px) and dual (12px) mode
- BlackHoleSetupWizard has 5 steps with Back/Next navigation matching OnboardingWizard pattern
- Wizard step 2 polls for BlackHole detection with QTimer(2000ms)
- Wizard step 3 guides user to route system audio output through BlackHole with "Open Sound Settings" and "Open Audio MIDI Setup" buttons
- Wizard step 4 creates Aggregate Device in background thread
- Design tokens updated with toggle dimensions
- All widget tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-system-audio-capture/02-02-SUMMARY.md`
</output>
